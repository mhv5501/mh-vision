[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NETLIFY_SKIP_SNIPPETS = "true"
  DISABLE_NETLIFY_DRAWER = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
